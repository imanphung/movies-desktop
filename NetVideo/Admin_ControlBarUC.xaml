<UserControl x:Class="NetVideo.Admin_ControlBarUC"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Name="controlbar">
    <Grid x:Name="GridBarraTitulo" Background="#FF2F2F40">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10,0">
            <Button  Command="{Binding CloseCommand}"
                    CommandParameter="{Binding ElementName=acontrolbar}"  Style="{StaticResource MaterialDesignFloatingActionMiniAccentButton}" Width="25" Height="25" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="White">
                <materialDesign:PackIcon Kind="Power"/>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>